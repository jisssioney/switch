# switch

从零实现的二层以太网交换机仿真与配置框架，仅用 Python 标准库、不联网。

- 入口：`python switch.py <子命令>`
- 所有时间相关行为（老化、收敛、超时）必须由显式事件时钟驱动；相同配置与相同帧序列必须产生逐字节相同的输出。
- 转发结果与统计统一写成 JSON，浮点数按固定小数位格式化。

## 测试

    python -m unittest discover
